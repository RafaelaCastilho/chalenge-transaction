package me.tgi.transaction.enumeration;

/**
 * Enum que representa os tipos de transações disponíveis.
 * Pode ser utilizado para indicar se uma transação é um depósito (DEPOSIT) ou um saque (WITHDRAWAL).
 */
public enum TypeTransaction {
	DEPOSIT, WITHDRAWAL
}
